package frc.robot.data;

public class ShooterDatum {
    public double m_topSpeed, m_middleSpeed, m_bottomSpeed;
    // public double distance;

    public ShooterDatum(double topSpeed, double middleSpeed, double bottomSpeed) {
        m_topSpeed = topSpeed;
        m_middleSpeed = middleSpeed;
        m_bottomSpeed = bottomSpeed;
    }
}